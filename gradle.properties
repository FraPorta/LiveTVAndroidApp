android.useAndroidX=true
android.enableJetifier=true
org.gradle.jvmargs=-Xmx4g

# Release Keystore Configuration
# Add your keystore details here after creating the keystore
# Uncomment and fill in the values:

# RELEASE_STORE_FILE=app/keystore/release.keystore
# RELEASE_STORE_PASSWORD=your_store_password_here
# RELEASE_KEY_ALIAS=key0
# RELEASE_KEY_PASSWORD=your_key_password_here

# Instructions:
# 1. Create keystore using Android Studio (Build -> Generate Signed Bundle/APK)
# 2. Place the keystore file in app/keystore/release.keystore
# 3. Uncomment the lines above and fill in your actual passwords
# 4. Keep this file secure and do not commit passwords to version control
